CREATE TABLE PROJECT
  (
    NAME VARCHAR(255) NOT NULL,
    SUMMARY VARCHAR(255) NOT NULL,
    UPDATED TIMESTAMP NOT NULL,
    PRIMARY KEY (NAME)
  );
  
  CREATE TABLE RELEASE
  (
    ID INT NOT NULL,
    PROJECT VARCHAR(255) NOT NULL,
    NAME VARCHAR(32) NOT NULL,
    DESCR VARCHAR(255) NOT NULL,
    UPDATED TIMESTAMP NOT NULL,
    PRIMARY KEY (ID),
    FOREIGN KEY (PROJECT)
      REFERENCES PROJECT (NAME)
  );
  
  CREATE TABLE REQUIREMENT
  (
    ID INT NOT NULL,
    RELEASE INT NOT NULL,
    NUMBER INT NOT NULL,
    DESCR VARCHAR(255) NOT NULL,
    UPDATED TIMESTAMP NOT NULL,
    PRIMARY KEY (ID),
    FOREIGN KEY (RELEASE)
      REFERENCES RELEASE (ID)
  );
  
 
INSERT INTO PROJECT (NAME, SUMMARY, UPDATED) VALUES ('sys', 'system module', CURDATE());
INSERT INTO PROJECT (NAME, SUMMARY, UPDATED) VALUES ('mt', 'threading module', CURDATE());
INSERT INTO PROJECT (NAME, SUMMARY, UPDATED) VALUES ('str', 'string module', CURDATE());


INSERT INTO RELEASE (ID, PROJECT, NAME, DESCR, UPDATED) VALUES (1, 'str', '1.0', 'first release', CURDATE());
INSERT INTO RELEASE (ID, PROJECT, NAME, DESCR, UPDATED) VALUES (2, 'mt', '1.0', 'first release', CURDATE());
INSERT INTO RELEASE (ID, PROJECT, NAME, DESCR, UPDATED) VALUES (3, 'sys', '1.0', 'first release', CURDATE());
INSERT INTO RELEASE (ID, PROJECT, NAME, DESCR, UPDATED) VALUES (4, 'sys', '2.0', '2nd release', CURDATE());
INSERT INTO RELEASE (ID, PROJECT, NAME, DESCR, UPDATED) VALUES (5, 'sys', '3.0', '3rd release', CURDATE());

INSERT INTO REQUIREMENT (ID, RELEASE, NUMBER, DESCR, UPDATED) VALUES (1, 1, 200, 'backend', CURDATE()); 
INSERT INTO REQUIREMENT (ID, RELEASE, NUMBER, DESCR, UPDATED) VALUES (2, 1, 201, 'frontend', CURDATE()); 
INSERT INTO REQUIREMENT (ID, RELEASE, NUMBER, DESCR, UPDATED) VALUES (3, 2, 100, 'build UI', CURDATE()); 
INSERT INTO REQUIREMENT (ID, RELEASE, NUMBER, DESCR, UPDATED) VALUES (4, 4, 100, 'fix bugs', CURDATE()); 
INSERT INTO REQUIREMENT (ID, RELEASE, NUMBER, DESCR, UPDATED) VALUES (5, 5, 100, 'delivery', CURDATE()); 
  